
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: helm-provider-clusterrolebinding
subjects:
  - kind: ServiceAccount
    name: provider-helm-239d2e74884b  # Replace this with the exact service account name from the error message
    namespace: crossplane-system
roleRef:
  kind: ClusterRole
  name: helm-provider-clusterrole
  apiGroup: rbac.authorization.k8s.io
